# OpenSim + PoseProgram pip 의존성 한 번에 설치
# conda env create -f environment.yml && conda activate human-3d-motion
name: human-3d-motion
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r requirements-gpu.txt
